# Uncomment this line to define a global platform for your project
platform :ios, '13.0' # Flutter often defaults to 13.0 or higher, keep as is.

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

# This line is essential for Flutter's build system integration with CocoaPods.
require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

# Sets up Flutter's default Podfile configurations before your target definitions.
flutter_ios_podfile_setup

target 'Runner' do
  # This installs all Flutter plugin pods. Do not remove this line.
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

  # PDFTron Pods - Keep these as they are explicitly added by you.
  use_frameworks! # Required for PDFTron
  pod 'PDFTron', podspec: 'https://pdftron.com/downloads/ios/flutter/pdftron/latest.podspec'
  pod 'PDFTronTools', podspec: 'https://pdftron.com/downloads/ios/flutter/pdftron-tools/latest.podspec'

  # Add any other specific pods your project needs here, for example:
  # pod 'AnotherPod', '~> 1.0.0'
end

# This block runs after all pods have been installed.
post_install do |installer|
  installer.pods_project.targets.each do |target|
    # Always apply Flutter's default build settings first for all targets.
    # This ensures Flutter's internal requirements are met for all pods.
    flutter_additional_ios_build_settings(target)

    # Disable ENABLE_BITCODE for all targets.
    # This is a common practice in Flutter/iOS development, especially with third-party libraries,
    # as Bitcode can sometimes cause issues or is not required for app store submission anymore.
    target.build_configurations.each do |config|
      config.build_settings['ENABLE_BITCODE'] = 'NO'
    end

    # --- REMOVED: Aggressive fix targeting specific privacy bundles including webview_flutter_wkwebview ---
    # The previous aggressive fix, which targeted specific privacy bundles like
    # 'webview_flutter_wkwebview-webview_flutter_wkwebview_privacy' and
    # 'shared_preferences_foundation-shared_preferences_foundation_privacy',
    # has been removed. This was likely a workaround for specific build issues
    # that are no longer necessary or are causing other problems.
    # Removing this makes the Podfile "safer" by not making highly specific,
    # potentially breaking modifications to Flutter's generated pod targets.
    # If you encounter build issues related to privacy bundles or Swift libraries
    # after this change, they would need to be addressed with a more targeted
    # and less destructive solution, or by updating the problematic plugins.
  end

  # CRITICAL: Save the modified project.
  # This line ensures that any changes made in the post_install block are written back to the .xcodeproj file.
  installer.pods_project.save

  # --- ULTIMATE LAST RESORT HACK: Direct file modification ---
  # This entire block was a highly fragile and dangerous last resort.
  # It has been removed to ensure the Podfile is "safe" and maintainable.
  # Direct modification of project.pbxproj files outside of Xcodeproj gem's
  # intended API is strongly discouraged as it can easily corrupt your project.
end
